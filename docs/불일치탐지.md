# 불일치 탐지 — 배포 가능한 산출물

작성 2026-09-07. `models/discrepancy.py` · `prediction/score.py` ·
`contracts/supplier-risk-score.v1.schema.json`

지금까지 민감도 분석에서 재던 「gap 복원」은 **정답값(`true_gap`)을 타깃으로 쓴
복원 가능성의 상한**이었다. 실데이터에서는 쓸 수 없다. 이 문서는 정답값 없이
관측만으로 계산하는 버전과, 그것이 실제로 무엇을 잡는지를 다룬다.

---

## 1. 왜 되는가

관측을 **누가 통제하느냐**로 피처를 가른다.

```
자기신고   공장이 직접 채점한 값 — 불량률 · 검사 결과 · 특이사항 유무
증거       공장이 형태를 바꾸기 어려운 값 — 우리가 기록한 시각, 물리 대조,
           실측 치수, 운영 실적
```

관측 모형이 이 방법의 근거다.

```
보고  r = internal × max(bias × noise, mes_floor)   ← bias 가 여기에만 걸린다. mes_floor 는 MES 하한(09-08)
증거  h ~ internal                     ← bias 와 무관하다
```

증거로 보고를 예측하는 함수를 **공장 전체에 걸쳐** 적합하면 그 함수는 평균적인
`internal → 보고` 관계를 배운다. 그러면 **잔차가 그 공장의 bias 편차**가 된다.

```
불일치 = log(증거가 예상한 보고) − log(실제 보고)
```

**정답값은 어디에도 들어가지 않는다.** `true_gap` 은 평가에만 쓴다.

---

## 2. ⚠ 실측 — 오더 단위로는 약하고 공장 단위로는 강하다

7 seed.

| 주장 | 평균 | 범위 | 부호 일관 |
| --- | --- | --- | --- |
| **공장 정직도 Spearman 상관** | **+0.673** | 0.52 ~ 0.84 | **7/7** |
| 불일치 ↔ 실제 축소량 (오더 단위) | +0.164 | −0.03 ~ 0.31 | 6/7 |
| 불일치 ↔ 예측 오차 | **−0.013** | −0.16 ~ 0.16 | **3/7** |

**한 오더의 불일치는 노이즈다. 같은 공장에서 반복되면 습관이다.**

> ⚠ **철회한 주장 하나.** 처음에는 "불일치 상위 10% 오더에서 예측 오차가 2.2배" 라고 썼다.
> **단일 시드 값이었다.** 7 seed 로 재니 Spearman 상관이 −0.013(3/7)으로 무너진다.
> 극단 꼬리에서만 약하게 보이는 것을 단조 관계로 일반화했던 것이다.

그리고 불일치는 **진짜 escape 와 음의 상관(−0.49)** 이다. 구조상 당연하다 —
불일치가 높다는 것은 정의상 보고가 낮다는 뜻이고, 보고는 실제 불량률과 상관되므로
불일치 상위 오더는 오히려 실제로는 나쁘지 않은 쪽으로 기운다.

**"불일치가 높다 = 이 오더가 불량 난다" 로 읽으면 틀린다.** 그건 위험 예측의 일이다.

---

## 3. 그래서 두 축을 다른 단위에서 판단한다

```
예측 위험     오더 단위     이 오더가 불량으로 끝날 것 같다   → 검사를 조인다
불일치        공장 단위     이 공장의 보고를 믿을 수 없다     → 가서 본다
```

현장 방문은 **공장 신뢰도를 게이트로 걸고, 오더 불일치를 순위로 쓴다.**
오더 하나의 불일치만으로 방문을 보내는 것은 근거가 없다.

> 첫 설계는 `불일치 상위 10% AND 예측 위험 상위 50%` 였다. **`site_visit` 이 0건 나왔다.**
> 두 축이 구조적으로 음의 상관이기 때문이다 — 불일치가 높으면 보고가 낮고,
> 모델은 보고를 믿으므로 위험을 낮게 준다. 조건을 AND 로 걸면 영원히 안 걸린다.

---

## 4. 결과 — 두 권고가 서로 다른 것을 잡는다

7 seed 평균, 테스트 270건 기준.

| 권고 | 건수 | 실제 상위 20% 위험 | 편향 심한 공장 |
| --- | --- | --- | --- |
| **현장 방문** | 33 | 7.0% | **95.6%** |
| **검사 강화** | 26 | **74.7%** | 32.2% |
| 전화 | 34 | 9.8% | 56.1% |
| 없음 | 178 | 14.4% | 45.4% |
| — 전체 기저율 | 270 | 18.5% | 51.3% |

- **검사 강화**는 실제로 나쁜 오더를 잡는다 — 74.7% vs 기저 18.5%, **4.0배**
- **현장 방문**은 못 믿을 공장을 잡는다 — 95.6% vs 기저 51.3%. 거의 놓치지 않는다

> ⚠ **위 표는 09-07 생성기(12곳 전부 보고) 기준이다.** 09-08 재설계 뒤 같은 규칙을 돌리면
> **270건 중 219건이 현장 방문**으로 나왔다. 원인: MES 가 없는 오더(75%)의 불일치가 0 으로 들어오고,
> 그 0 이 분위수 컷(0.75·0.80)과 공장 신뢰도 게이트(0.75)를 전부 통과했다 — 컷 자체가 0 이 되고
> 증거 없는 공장 9곳이 모두 "못 믿을 공장" 이 됐다. **모르는 것과 못 믿는 것은 다르다.**
>
> **09-08 규칙**(`score.py`): 불일치 분위수·공장 신뢰도는 **보고가 있는 오더에서만** 계산한다.
> 보고가 없는 오더는 `site_visit`·`call` 대상이 아니고 `discrepancy_flag` 도 False 다 — 예측 위험(`tighten_inspection`)만 걸린다.
> `factory_trust()` 도 보고 없는 오더를 빼므로 MES 없는 공장은 신뢰도 표에 나오지 않는다.
>
> 재채점(seed 20260907): **없음 227 · 검사 강화 22 · 현장 방문 11 · 전화 10.**
> 유형별 — a(64): 방문 11 · 전화 10 · 없음 43 / b(85): 없음 85 / c(121): 검사 강화 22 · 없음 99.
> 검사 강화가 전부 유형 c 에 몰린 것은 이 seed 에서 c 공장들의 진실 escape 중앙값이 1,249 PPM(a·b 는 ~380)이기 때문이다.

### 4-1. 09-08 생성기에서 다시 잰 정밀도 (8 seed, `scripts/discrepancy_precision.py`, 채점 파이프라인과 같은 예측기)

| 권고 | 건수 | 실제 상위 20% 위험 | 편향 심한 공장 |
| --- | --- | --- | --- |
| 현장 방문 | 6.8 | 0.0% | **25.0%** |
| **검사 강화** | 23.1 | **77.4%** | 39.7% |
| 전화 | 8.9 | 6.2% | 41.7% |
| 없음 | 231.2 | 15.5% | 51.7% |
| — 기저율 전체 | 270 | 20.0% | 49.3% |
| — 기저율 유형 a (보고 있는 오더) | | | 39.9% |

"편향 심한 공장" = 실효 편향 `max(report_bias, mes_input_bias)` 가 12곳 중앙값 아래.

- **검사 강화 축은 유지된다** — 77.4% vs 기저 20%, **3.9배**(09-07: 74.7%, 4.0배). 생성기가 바뀌어도 흔들리지 않는 축이다.
- **현장 방문 축은 무너졌다** — 편향 심한 공장 25.0% vs 유형 a 기저 39.9%. **기저보다 낮다.** 09-07 의 95.6% 는 12곳 전부 보고할 때의 값이다.
  공장 정직도 Spearman 은 3곳 위의 값이라 −1~+1 사이를 오간다(평균 +0.31, 6/8) — 통계가 성립하지 않는다.

**왜 무너졌나 — 원인이 둘이고, 갈라서 재야 보인다** (`scripts/coverage_sweep.py`, 8 seed, MES 공장 수 sweep):

| MES 공장 수 (a/b/c) | 방문 건수 | 방문 정밀도 | 기저 (보고 공장 내) | 정직도 Spearman (양수 seed) | 검사 강화 | 전체 순위상관 | a 순위상관 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 3 (3/4/5) | 6.8 | 37.5% | 31.5% | +0.31 (6/8) | 77.4% | +0.661 | +0.851 |
| 6 (6/3/3) | 15.4 | 59.0% | 49.9% | +0.14 (5/8) | 82.2% | +0.725 | +0.840 |
| 9 (9/2/1) | 25.4 | 49.8% | 42.9% | +0.17 (6/8) | 85.8% | +0.792 | +0.849 |
| 12 (12/0/0) | 30.2 | **61.8%** | 49.3% | +0.24 (8/8) | **92.5%** | **+0.868** | +0.868 |
| **12, MES 하한 OFF** (`--no-floor`) | 32.9 | **94.9%** | 50.5% | **+0.58** (8/8) | 77.4% | +0.721 | +0.721 |
| 3, MES 하한 OFF | 7.0 | 37.5% | 32.4% | +0.31 (6/8) | 70.6% | +0.610 | +0.664 |

1. **표본 효과.** 보고 공장 3곳이면 하한을 켜든 끄든 방문 정밀도가 기저 +5pt 다. §1 의 방법은 "증거 → 보고"
   관계를 공장 전체에 걸쳐 적합하고 잔차를 공장 편향으로 읽는데, 공장 수준이 3개면 편향과 역량이 분리되지 않는다.
2. **MES 하한 효과.** 12곳 전부 보고해도 하한을 켜면 61.8% / r +0.24, 끄면 **94.9% / r +0.58** — 09-07 의 95.6% / +0.673 이
   하한 OFF 에서 그대로 재현된다. 하한(`mes_input_bias` 0.55~1.0)이 거의 항상 구속되면 "곤란할수록 더 숨긴다"
   (`bias_sensitivity × trouble`)가 잘려 나가고, 불일치 탐지의 증거 피처(잔업↔불량 · 침묵↔지연)는 바로 그 곤란 의존 은폐를 잡는 것이다.

**그리고 하한은 예측 쪽을 올린다** — 12곳에서 전체 순위상관 0.721 → 0.868, 검사 강화 77 → 92%. 보고가 정직할수록
L1 예측 기여는 오르고 불일치 탐지는 내린다(§6-1 09-07 관찰 "두 기능이 서로를 덮는다"의 재확인).

**따라오는 결론 둘.**
- **불일치 탐지가 성립하는 조건은 "MES 공장이 많다" 하나가 아니다. "MES 입력이 얼마나 정직한가"(`mes_input_bias_range`)가
  같은 무게로 걸려 있다.** 이 값은 실측 근거 없는 가정이다(생성기_캘리브레이션 §2.4). 민감도 축에 넣었고 CTO 질문 29번으로 올렸다 —
  "MES 에 찍히는 폐기·재작업 수량이 실제와 얼마나 다른가". 답이 "거의 정직"이면 핵심 상품은 예측이고 불일치 탐지는 보조,
  "종이 보고와 비슷"이면 반대다. **어느 쪽이든 하나는 작동한다** — 그것이 이 설계가 가정에 대해 강건한 이유다.
- 예측 쪽 설치 가치는 가정과 무관하게 단조다: **MES 공장 수 3 → 6 → 9 → 12 에 전체 순위상관 0.66 → 0.73 → 0.79 → 0.87.**
  이것이 설치 영업의 숫자다. 불일치 탐지는 12곳·정직도 가정이 맞을 때만 두 번째 논거가 된다.

둘이 겹치지 않는 것이 설계 의도다. 하나는 **품질**을, 하나는 **신뢰**를 본다.

---

## 5. 출력 계약

`supplier-risk-score.v1` — 값이 하나가 아니라 셋이다.

```json
{
  "schema_version": "supplier_risk.score.v1",
  "order_id": "ord_00633",
  "factory_id": "fac_kr_01",
  "scored_at": "2026-02-07T15:13:23Z",
  "predicted_escape_ppm": 312.2,
  "risk_level": "low",
  "reported_defect_rate": 0.00571,
  "discrepancy": 1.4494,
  "discrepancy_flag": true,
  "factory_trust_low": true,
  "recommended_action": "site_visit",
  "action_reason": "보고 신뢰도가 낮은 공장이고, 이 오더도 증거와 어긋난다"
}
```

- `predicted_escape_ppm` 은 **PPM 단위**다. 구매·품질 담당자가 쓰는 문법이라
  설명 없이 읽힌다(세계 수준 100 PPM 이하, 오늘날 고객 기대 1,000 PPM 수준)
- `reported_defect_rate` 는 보고가 없으면 **null 이다. 0 이 아니다** — 결측과 무결함은 다르다
- `tighten_inspection` 은 입고검사 강화 또는 **제3자 DUPRO**(생산 30~50% 시점 현장 검사)
  발주를 뜻한다. 개입이면서 동시에 고품질 관측을 얻는 수단이다

⚠ FactoryOS 의 `quality-prediction.v1` 은 `score_type` 이 `quality_fail` 고정이고
추가 필드를 금지해서 이 모양이 들어가지 않는다. **우리가 새 계약을 정의해도 되는지는
FactoryOS 담당자 확인이 필요하다**(CTO 질문 18번).

---

## 6. 확인할 수 없는 가정 하나

**나쁜 공장이 더 숨기는가?** 지금 생성기는 `bias_capability_corr: 0.0` — **독립**으로 둔다.
상관을 −0.85 까지 넣어 보면 이렇게 된다.

| 상관 | 현장방문의 위험 적중 | 검사강화의 위험 적중 | 정직도 Spearman 상관 |
| --- | --- | --- | --- |
| 0.00 (독립) | 1.2% | 70.9% | **+0.573** |
| −0.50 | 1.2% | 70.7% | +0.259 |
| −0.85 | 2.2% | 71.6% | +0.061 |

**검사 강화 축은 어떤 가정에서도 71% 로 흔들리지 않는다.**
반면 불일치의 정직도 판별력은 상관이 커질수록 떨어진다 — 편향과 품질이 얽히면
잔차가 편향을 분리하지 못하기 때문이다.

→ **CTO 확인 항목 22번**: 보고를 부실하게 하는 공장이 품질도 나쁩니까?

---

## 7. 사유 분해 *(2026-09-07 완료)*

불일치 스칼라 하나로는 **"가서 뭘 봐야 하는가"** 를 말할 수 없다. 짝마다 통계를 따로 두고
어느 짝이 어긋났는지를 낸다. 다섯 짝 전부 **값이 클수록 의심스럽게** 방향을 통일해
threshold를 하나로 쓴다(학습 구간 p85).

| 사유 | 관측치 | 확인할 것 |
| --- | --- | --- |
| `qty_inflation` 보고 수량이 자재 소진량과 맞지 않는다 | `l1_material_gap` | 재공품 실사 · 자재 입출고 대장 |
| `defect_hiding` 불량은 적다는데 잔업이 많다 | `l1_ot_vs_reject` | 재작업 대장 · 특별공정 기록 |
| `optimistic_due` 지금 속도로는 보고한 납기를 맞출 수 없다 | `l1_pace_gap` | 설비 가동 계획 · 잔여 공정 일정 |
| `silent_trouble` 특이사항은 없다는데 보고가 계속 늦는다 | `l1_silent_delay` | 담당자 면담 · 현재 공정 단계 |
| `stale_evidence` 보낸 사진이 그 주에 찍힌 것이 아니다 | `l1_photo_stale` | 현장 사진 즉석 촬영 요청 |
| **`no_evidence`** 대조할 증거가 하나도 없다 | — | T2 항목 제출 요구 |

여섯 번째는 짝이 아니다. **대조할 것이 없다는 것 자체가 사유다** — T2 를 안 낸 공장은
우리가 검증할 수단이 없다는 뜻이고 그건 "괜찮다" 가 아니다. 이걸 넣기 전에는
사유 없는 현장 방문이 **35% 남았고**, 넣은 뒤 **23%** 가 됐다.

### 검증 — 사유가 맞는가

**분해가 맞는지 확인할 방법이 없으면 "어느 짝이 어긋났다" 는 주장을 할 수 없다.**
그래서 생성기가 메커니즘별 정답값(`mech_*`)을 남기게 고쳤다 — 수량 부풀리기 계수,
불량 축소 계수, 납기 고수 일수, 이상 신고 억제 회차, 사진 재사용 횟수.
실데이터에서는 영원히 알 수 없는 값이다.

| 사유 | 관측치 ↔ 메커니즘 (Spearman) | 발동 시 메커니즘 심화 |
| --- | --- | --- |
| `stale_evidence` | **+0.753** | **15.4배** |
| `optimistic_due` | +0.491 | 2.03배 |
| `defect_hiding` | −0.462 | 2.05배 |
| `qty_inflation` | +0.424 | 1.23배 |
| `silent_trouble` | +0.219 | 2.42배 |

⚠ `qty_inflation` 만 배수가 낮다. 방향은 맞지만(r=0.42) **생성기의 부풀리기 진폭 자체가
작다**(중앙 2.8% · p90 6.1%). 실제 공장이 더 크게 부풀린다면 배수도 올라간다 — 가정이다.

> **이 검증이 실제 버그를 잡았다.** `silent_delay` 를 처음에 `issue_any`(한 번이라도
> 신고했나)로 재고 있었는데, 곤란한 공장은 여러 회차 중 한 번은 신고하게 되므로
> 신호가 상수 0 이 되어 **상관 부호가 뒤집혀 있었다(−0.55)**. 신고 **비율**로 바꿔 +0.22 가 됐다.
> 정답값 없이는 이 버그를 영원히 못 찾았을 것이다.
> `tests/unit/test_reasons.py` 가 다섯 짝의 부호를 고정한다.

---

## 8. 남은 것

1. **공장 신뢰도의 시간 가중** — 지금은 전 기간 평균이다. 담당자가 바뀌면 습관도 바뀐다
2. seed 5~10개로 전체 재측정
3. `qty_inflation` 의 진폭 — CTO 확인 대상. 실제로 수량을 얼마나 부풀리나
