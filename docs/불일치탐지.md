# 불일치 탐지 — 배포 가능한 산출물

작성 2026-09-07. `models/discrepancy.py` · `prediction/score.py` ·
`contracts/supplier-risk-score.v1.schema.json`

지금까지 민감도 분석에서 재던 「gap 복원」은 **정답값(`true_gap`)을 타깃으로 쓴
복원 가능성의 상한**이었다. 실데이터에서는 쓸 수 없다. 이 문서는 정답값 없이
관측만으로 계산하는 버전과, 그것이 실제로 무엇을 잡는지를 다룬다.

---

## 1. 왜 되는가

관측을 **누가 통제하느냐**로 피처를 가른다.

```
자기신고   공장이 직접 채점한 값 — 불량률 · 검사 결과 · 특이사항 유무
증거       공장이 형태를 바꾸기 어려운 값 — 우리가 기록한 시각, 물리 대조,
           실측 치수, 운영 실적
```

관측 모형이 이 방법의 근거다.

```
보고  r = internal × max(bias × noise, mes_floor)   ← bias 가 여기에만 걸린다. mes_floor 는 MES 하한(09-08)
증거  h ~ internal                     ← bias 와 무관하다
```

증거로 보고를 예측하는 함수를 **공장 전체에 걸쳐** 적합하면 그 함수는 평균적인
`internal → 보고` 관계를 배운다. 그러면 **잔차가 그 공장의 bias 편차**가 된다.

```
불일치 = log(증거가 예상한 보고) − log(실제 보고)
```

**정답값은 어디에도 들어가지 않는다.** `true_gap` 은 평가에만 쓴다.

---

## 2. ⚠ 실측 — 오더 단위로는 약하고 공장 단위로는 강하다

7 seed.

| 주장 | 평균 | 범위 | 부호 일관 |
| --- | --- | --- | --- |
| **공장 정직도 Spearman 상관** | **+0.673** | 0.52 ~ 0.84 | **7/7** |
| 불일치 ↔ 실제 축소량 (오더 단위) | +0.164 | −0.03 ~ 0.31 | 6/7 |
| 불일치 ↔ 예측 오차 | **−0.013** | −0.16 ~ 0.16 | **3/7** |

**한 오더의 불일치는 노이즈다. 같은 공장에서 반복되면 습관이다.**

> ⚠ **철회한 주장 하나.** 처음에는 "불일치 상위 10% 오더에서 예측 오차가 2.2배" 라고 썼다.
> **단일 시드 값이었다.** 7 seed 로 재니 Spearman 상관이 −0.013(3/7)으로 무너진다.
> 극단 꼬리에서만 약하게 보이는 것을 단조 관계로 일반화했던 것이다.

그리고 불일치는 **진짜 escape 와 음의 상관(−0.49)** 이다. 구조상 당연하다 —
불일치가 높다는 것은 정의상 보고가 낮다는 뜻이고, 보고는 실제 불량률과 상관되므로
불일치 상위 오더는 오히려 실제로는 나쁘지 않은 쪽으로 기운다.

**"불일치가 높다 = 이 오더가 불량 난다" 로 읽으면 틀린다.** 그건 위험 예측의 일이다.

---

## 3. 그래서 두 축을 다른 단위에서 판단한다

```
예측 위험     오더 단위     이 오더가 불량으로 끝날 것 같다   → 품질 검토
불일치        공장 단위     이 공장의 보고를 믿을 수 없다     → 보고 검토
```

(09-08 저녁부터 권고 코드는 `review_needed` 하나다 — §9. 아래 §4·§4-1 의 세 갈래 명칭은 당시 측정 기록이다.)

불일치 쪽 검토는 **공장 신뢰도를 게이트로 걸고, 오더 불일치를 순위로 쓴다.**
오더 하나의 불일치만으로 검토를 거는 것은 근거가 없다.

> 첫 설계는 `불일치 상위 10% AND 예측 위험 상위 50%` 였다. **`site_visit` 이 0건 나왔다.**
> 두 축이 구조적으로 음의 상관이기 때문이다 — 불일치가 높으면 보고가 낮고,
> 모델은 보고를 믿으므로 위험을 낮게 준다. 조건을 AND 로 걸면 영원히 안 걸린다.

---

## 4. 결과 — 두 권고가 서로 다른 것을 잡는다

7 seed 평균, 테스트 270건 기준.

| 권고 | 건수 | 실제 상위 20% 위험 | 편향 심한 공장 |
| --- | --- | --- | --- |
| **현장 방문** | 33 | 7.0% | **95.6%** |
| **검사 강화** | 26 | **74.7%** | 32.2% |
| 전화 | 34 | 9.8% | 56.1% |
| 없음 | 178 | 14.4% | 45.4% |
| — 전체 기저율 | 270 | 18.5% | 51.3% |

- **검사 강화**는 실제로 나쁜 오더를 잡는다 — 74.7% vs 기저 18.5%, **4.0배**
- **현장 방문**은 못 믿을 공장을 잡는다 — 95.6% vs 기저 51.3%. 거의 놓치지 않는다

> ⚠ **위 표는 09-07 생성기(12곳 전부 보고) 기준이다.** 09-08 재설계 뒤 같은 규칙을 돌리면
> **270건 중 219건이 현장 방문**으로 나왔다. 원인: MES 미연동 오더(75%)의 불일치가 0 으로 들어오고,
> 그 0 이 분위수 컷(0.75·0.80)과 공장 신뢰도 게이트(0.75)를 전부 통과했다 — 컷 자체가 0 이 되고
> 증거 없는 공장 9곳이 모두 "못 믿을 공장" 이 됐다. **모르는 것과 못 믿는 것은 다르다.**
>
> **09-08 규칙**(`score.py`): 불일치 분위수·공장 신뢰도는 **보고가 있는 오더에서만** 계산한다.
> 보고가 없는 오더는 불일치 기준 검토 대상이 아니고 `discrepancy_flag` 도 False 다 — 예측 위험 기준으로만 걸린다.
> `factory_trust()` 도 보고 없는 오더를 빼므로 MES 미연동 공장은 신뢰도 표에 나오지 않는다.
>
> 재채점(seed 20260907): **없음 227 · 검사 강화 22 · 현장 방문 11 · 전화 10.**
> 유형별 — a(64): 방문 11 · 전화 10 · 없음 43 / b 미연동(206): 검사 강화 22 · 없음 184.

### 4-0. ⚠ 권고 코드가 하나가 됐다 (09-08 저녁)

`call` · `tighten_inspection` · `site_visit` 세 코드를 **`review_needed`(검토 필요)** 하나로 합쳤다.
무엇을 할지는 담당자가 정하고 계약은 근거만 준다는 취지다. 발동 조건과 임계값(0.75 · 0.80 · 0.90 분위)은 그대로다.

**그런데 지표가 코드에 묶여 있었다.** 「검사 강화가 실제 나쁜 오더를 잡는 비율」과 「현장 방문이 편향 공장을 잡는 비율」은
서로 다른 모집단이고, 합치면 두 집단의 평균이 되어 어느 쪽도 재지 못한다(실측: 합산 37% · 26%).
그래서 **`action_trigger`** 를 계약에 남겼다 — `risk` / `discrepancy_low_trust` / `discrepancy`. **코드는 하나, 측정은 셋이다.**

### 4-1. 축별 정밀도 (8 seed, `scripts/legacy/trigger_precision.py`, `docs/_축별_정밀도_표.md`)

**MES 12곳 시나리오** — 기저: 실제 위험 20.2% · 편향 심한 공장 49.9%

| 걸린 축 | 건수 | 실제 나쁜 오더 적중 | 편향 심한 공장 적중 |
| --- | --- | --- | --- |
| **`risk`** 예측 위험 상위 10% (구 검사 강화) | 8.0 | **95.4%** | 41.5% |
| **`discrepancy_low_trust`** 저신뢰 공장 + 불일치 상위 20% (구 현장 방문) | 10.0 | 2.7% | **60.6%** |
| `discrepancy` 증거 있는 오더의 불일치 상위 25% (구 전화) | 14.6 | 6.1% | 46.8% |

**MES 3곳 (현재)** — 기저: 실제 위험 20.2% · 편향 심한 공장 33.8%

| 걸린 축 | 건수 | 실제 나쁜 오더 적중 | 편향 심한 공장 적중 |
| --- | --- | --- | --- |
| **`risk`** | 8.8 | **65.7%** | 22.0% |
| `discrepancy_low_trust` | 3.0 | 7.1% | 28.6% — **기저 아래** |
| `discrepancy` | 4.8 | 3.5% | 22.9% — 기저 아래 |

"편향 심한 공장" = 실효 편향 `max(report_bias, mes_input_bias)` 가 보고 공장 중앙값 아래.

- **위험 축은 유지된다** — 12곳 95.4% vs 기저 20.2%, **4.7배**. 3곳에서도 65.7% (3.3배)로 흔들리지 않는다.
- **불일치 축은 3곳에서 성립하지 않는다** — 28.6% vs 기저 33.8%, 기저보다 낮다. 12곳에서 60.6%(+10.7pt)로 살아난다.
  공장 정직도 Spearman 도 3곳에서는 +0.06(4/8)으로 통계가 서지 않는다.

**왜 무너졌나 — 원인이 둘이고, 갈라서 재야 보인다** (`scripts/legacy/coverage_sweep.py`, 8 seed, MES 공장 수 sweep):

**축 분리 후 재생성** (`docs/_coverage_sweep_3_6_9_12.md`, 8 seed · 직렬 발주 생성기 332 오더)

| MES 연동 공장 수 | 위험축 정밀도 | 불일치축 건수 | 불일치축 정밀도 | 기저 | 정직도 Spearman (양수 seed) | 전체 순위상관 |
| --- | --- | --- | --- | --- | --- | --- |
| 3 (3/9) | 65.7% | 7.8 | 30.0% | 33.8% | +0.062 (4/8) | +0.519 |
| 6 (6/6) | 74.3% | 12.2 | 49.4% | 50.7% | +0.029 (4/8) | +0.596 |
| 9 (9/3) | 82.9% | 19.2 | 43.6% | 44.1% | +0.108 (4/8) | +0.688 |
| 12 (12/0) | **95.4%** | 24.6 | **51.6%** | 49.9% | **+0.221 (7/8)** | **+0.807** |

불일치축은 12곳에서야 기저를 넘고(+1.7pt), 그 안에서도 저신뢰 게이트를 통과한 것만 강하다(60.6%, +10.7pt).

아래는 축을 합쳐 재던 09-08 오후의 표다 — **모집단이 섞여 어느 축의 값도 아니다.** 대조용으로만 남긴다.

| MES 연동 공장 수 (a/b) | 방문 건수 | 방문 정밀도 | 기저 (보고 공장 내) | 정직도 Spearman (양수 seed) | 검사 강화 | 전체 순위상관 | a 순위상관 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 3 (3/9) | 6.8 | 37.5% | 31.5% | +0.31 (6/8) | 77.2% | +0.664 | +0.852 |
| 12 (12/0) | 30.2 | **61.8%** | 49.3% | +0.24 (8/8) | **92.5%** | **+0.868** | +0.868 |
| **12, MES 하한 OFF** | 32.9 | **94.9%** | 50.5% | **+0.58** (8/8) | 77.4% | +0.721 | +0.721 |

1. **표본 효과.** 보고 공장 3곳이면 하한을 켜든 끄든 방문 정밀도가 기저 +5pt 다. §1 의 방법은 "증거 → 보고"
   관계를 공장 전체에 걸쳐 적합하고 잔차를 공장 편향으로 읽는데, 공장 수준이 3개면 편향과 역량이 분리되지 않는다.
2. **MES 하한 효과.** 12곳 전부 보고해도 하한을 켜면 61.8% / r +0.24, 끄면 **94.9% / r +0.58** — 09-07 의 95.6% / +0.673 이
   하한 OFF 에서 그대로 재현된다. 하한(`mes_input_bias` 0.55~1.0)이 거의 항상 구속되면 "곤란할수록 더 숨긴다"
   (`bias_sensitivity × trouble`)가 잘려 나가고, 불일치 탐지의 증거 피처(잔업↔불량 · 침묵↔지연)는 바로 그 곤란 의존 은폐를 잡는 것이다.

**하한을 더 올려 재면 예측은 포화돼 있다** (`docs/_하한_시나리오_표.md`, 8 seed). 하한 0.55 → 0.95 에서
12곳 전체 순위상관 +0.807 → +0.819(**+0.01**), 위험축 정밀도 95.4 → 93.0%. 그런데 불일치축은 51.6 → 46.1%(기저 49.7 **아래**),
공장 정직도 r 은 +0.221(7/8) → **−0.099(2/8)** 로 무너진다.

**즉 정직한 세계에서는 불일치 탐지가 상품이 아니다.** 예측 이득은 0.02~0.55 구간에서 이미 다 나왔고, 그 위로는 없다.
그리고 09-08 저녁 확인 — **FactoryOS 에 사람이 입력하는 경로는 없고 MES/ERP API 에서 자동 수집한다** — 은
하한을 올리는 쪽 증거다. 따라서 **주력 상품은 예측이고, 불일치 탐지는 MES 입력이 부정직한 공장에서만 작동하는 보조 기능**이다.

**따라오는 결론 둘.**
- **불일치 탐지가 성립하는 조건은 "MES 연동 공장이 많다" 하나가 아니다. "MES 입력이 얼마나 정직한가"(`mes_input_bias_range`)가
  같은 무게로 걸려 있다.** 이 값은 실측 근거 없는 가정이다(생성기_캘리브레이션 §2.4). 민감도 축에 넣었고 CTO 질문 29번으로 올렸다 —
  "MES 에 찍히는 폐기·재작업 수량이 실제와 얼마나 다른가". 답이 "거의 정직"이면 핵심 상품은 예측이고 불일치 탐지는 보조,
  "종이 보고와 비슷"이면 반대다. **어느 쪽이든 하나는 작동한다** — 그것이 이 설계가 가정에 대해 강건한 이유다.
- 예측 쪽 연동 가치는 가정과 무관하게 단조다: **MES 연동 공장 3 → 6 → 9 → 12 에 전체 순위상관 0.66 → 0.73 → 0.79 → 0.87.**
  CellOS·FactoryOS 는 협력 조건이라 이미 12곳 전부 있다 — 남은 레버는 **자체 MES 연동(또는 FactoryOS 입력 정착) 9곳 확대**이고, 이 곡선이 그 우선순위의 근거다(CTO 질문 19 로드맵).
  불일치 탐지는 12곳 연동·정직도 가정이 맞을 때 두 번째로 성립한다.

둘이 겹치지 않는 것이 설계 의도다. 하나는 **품질**을, 하나는 **신뢰**를 본다.

---

## 5. 출력 계약

`supplier-risk-score.v1` — 값이 하나가 아니라 셋이다.

```json
{
  "schema_version": "supplier_risk.score.v1",
  "order_id": "ord_00633",
  "factory_id": "fac_kr_01",
  "scored_at": "2026-02-07T15:13:23Z",
  "predicted_escape_ppm": 312.2,
  "risk_level": "low",
  "reported_defect_rate": 0.00571,
  "discrepancy": 1.4494,
  "discrepancy_flag": true,
  "factory_trust_low": true,
  "recommended_action": "review_needed",
  "action_reason": "보고 신뢰도가 낮은 공장이고, 이 오더도 증거와 어긋난다"
}
```

- `predicted_escape_ppm` 은 **PPM 단위**다. 구매·품질 담당자가 쓰는 문법이라
  설명 없이 읽힌다(세계 수준 100 PPM 이하, 오늘날 고객 기대 1,000 PPM 수준)
- `reported_defect_rate` 는 보고가 없으면 **null 이다. 0 이 아니다** — 결측과 무결함은 다르다
- `recommended_action` 은 `none` / `review_needed` 둘이다(09-08 저녁, §9). 무엇을 할지는 계약이 정하지 않고
  `action_reason`·`action_check` 로 근거만 준다

⚠ FactoryOS 의 `quality-prediction.v1` 은 `score_type` 이 `quality_fail` 고정이고
추가 필드를 금지해서 이 모양이 들어가지 않는다. **우리가 새 계약을 정의해도 되는지는
FactoryOS 담당자 확인이 필요하다**(CTO 질문 18번).

---

## 6. 확인할 수 없는 가정 하나

**나쁜 공장이 더 숨기는가?** 지금 생성기는 `bias_capability_corr: 0.0` — **독립**으로 둔다.
상관을 −0.85 까지 넣어 보면 이렇게 된다.

| 상관 | 현장방문의 위험 적중 | 검사강화의 위험 적중 | 정직도 Spearman 상관 |
| --- | --- | --- | --- |
| 0.00 (독립) | 1.2% | 70.9% | **+0.573** |
| −0.50 | 1.2% | 70.7% | +0.259 |
| −0.85 | 2.2% | 71.6% | +0.061 |

**검사 강화 축은 어떤 가정에서도 71% 로 흔들리지 않는다.**
반면 불일치의 정직도 판별력은 상관이 커질수록 떨어진다 — 편향과 품질이 얽히면
잔차가 편향을 분리하지 못하기 때문이다.

→ **CTO 확인 항목 22번**: 보고를 부실하게 하는 공장이 품질도 나쁩니까?

---

## 7. 사유 분해 *(2026-09-07 완료)*

불일치 스칼라 하나로는 **"가서 뭘 봐야 하는가"** 를 말할 수 없다. 짝마다 통계를 따로 두고
어느 짝이 어긋났는지를 낸다. 다섯 짝 전부 **값이 클수록 의심스럽게** 방향을 통일해
threshold를 하나로 쓴다(학습 구간 p85).

| 사유 | 관측치 | 확인할 것 |
| --- | --- | --- |
| `qty_inflation` 보고 수량이 자재 소진량과 맞지 않는다 | `l1_material_gap` | 재공품 실사 · 자재 입출고 대장 |
| `defect_hiding` 불량은 적다는데 잔업이 많다 | `l1_ot_vs_reject` | 재작업 대장 · 특별공정 기록 |
| `optimistic_due` 지금 속도로는 보고한 납기를 맞출 수 없다 | `l1_pace_gap` | 설비 가동 계획 · 잔여 공정 일정 |
| `silent_trouble` 특이사항은 없다는데 보고가 계속 늦는다 | `l1_silent_delay` | 담당자 면담 · 현재 공정 단계 |
| `stale_evidence` 보낸 사진이 그 주에 찍힌 것이 아니다 | `l1_photo_stale` | 현장 사진 즉석 촬영 요청 |
| **`no_evidence`** 대조할 증거가 하나도 없다 | — | T2 항목 제출 요구 |

여섯 번째는 짝이 아니다. **대조할 것이 없다는 것 자체가 사유다** — T2 를 안 낸 공장은
우리가 검증할 수단이 없다는 뜻이고 그건 "괜찮다" 가 아니다. 이걸 넣기 전에는
사유 없는 현장 방문이 **35% 남았고**, 넣은 뒤 **23%** 가 됐다.

### 검증 — 사유가 맞는가

**분해가 맞는지 확인할 방법이 없으면 "어느 짝이 어긋났다" 는 주장을 할 수 없다.**
그래서 생성기가 메커니즘별 정답값(`mech_*`)을 남기게 고쳤다 — 수량 부풀리기 계수,
불량 축소 계수, 납기 고수 일수, 이상 신고 억제 회차, 사진 재사용 횟수.
실데이터에서는 영원히 알 수 없는 값이다.

| 사유 | 관측치 ↔ 메커니즘 (Spearman) | 발동 시 메커니즘 심화 |
| --- | --- | --- |
| `stale_evidence` | **+0.753** | **15.4배** |
| `optimistic_due` | +0.491 | 2.03배 |
| `defect_hiding` | −0.462 | 2.05배 |
| `qty_inflation` | +0.424 | 1.23배 |
| `silent_trouble` | +0.219 | 2.42배 |

⚠ `qty_inflation` 만 배수가 낮다. 방향은 맞지만(r=0.42) **생성기의 부풀리기 진폭 자체가
작다**(중앙 2.8% · p90 6.1%). 실제 공장이 더 크게 부풀린다면 배수도 올라간다 — 가정이다.

> **이 검증이 실제 버그를 잡았다.** `silent_delay` 를 처음에 `issue_any`(한 번이라도
> 신고했나)로 재고 있었는데, 곤란한 공장은 여러 회차 중 한 번은 신고하게 되므로
> 신호가 상수 0 이 되어 **상관 부호가 뒤집혀 있었다(−0.55)**. 신고 **비율**로 바꿔 +0.22 가 됐다.
> 정답값 없이는 이 버그를 영원히 못 찾았을 것이다.
> `tests/unit/test_reasons.py` 가 다섯 짝의 부호를 고정한다.

---

## 8. 남은 것

1. **공장 신뢰도의 시간 가중** — 지금은 전 기간 평균이다. 담당자가 바뀌면 습관도 바뀐다
2. seed 5~10개로 전체 재측정
3. `qty_inflation` 의 진폭 — CTO 확인 대상. 실제로 수량을 얼마나 부풀리나

---

## 9. 09-08 저녁 — 권고 코드를 `review_needed` 하나로 합쳤다

사용자 결정(화면 + 계약 + 모델 출력). 세 임계(신뢰 낮은 공장 × 큰 불일치 · 예측 위험 상위 10% · 증거 있는 오더의 불일치 임계)는
그대로 두고 **출력 코드만 하나**다. 사유·확인할 것은 걸린 임계에 따라 그대로 다르다.

8 seed 재측정(`scripts/legacy/discrepancy_precision.py`, 보고 공장 3곳):

| 권고 | 건수 | 실제 상위 20% 위험 | 편향 심한 공장 |
| --- | --- | --- | --- |
| `review_needed` (전체 오더) | 38.5 | **46.2%** | 37.0% |
| `review_needed` (보고 있는 오더) | 24.5 | | 39.0% |
| 없음 | 231.5 | 15.5% | 51.6% |
| — 기저율 전체 | 270 | 20.0% | 49.3% |
| — 기저율 유형 a | | | 39.9% |

12곳 전부 연동(`coverage_sweep.py 12`): 보고 오더의 검토 정밀도 49.8% vs 기저 49.3% · 전체 위험 정밀도 26.5% · 순위상관 0.868(변화 없음).

**합치면 두 축의 정밀도가 서로를 희석한다.** 예측 위험 축 단독 77.2% → 46.2%(기저 2.3배, 이전 3.9배).
편향 공장 정밀도는 3곳 조건에서 이미 기저였고, 12곳 조건의 94.9% 는 49.8% 로 기저와 같아진다 — 예측 위험으로 걸린 오더가
편향과 무관하게 섞이기 때문이다. 순위상관과 사유 분해는 영향이 없다(권고 코드와 무관한 값).
축별 정밀도가 필요하면 `risk_level ≥ high` / `discrepancy_flag` 로 사후에 가를 수 있다 — 코드를 다시 나누지 않아도 된다.

---

## 10. 09-08 저녁 — 한 공장에 한 오더 (데이터 전제 전환)

사용자 전제: **협력 공장은 보통 한 번에 오더 하나만 돈다.** 생성기를 공장별 직렬 발주로 바꿨다(`orders.py` — 다음 발주 = 이전 약속 납기 + 0~5일).
같은 공장의 연속 오더가 겹치지 않으므로 `load_index`(동시 진행 오더 수)는 상수 0 → 피처·잠재변수·불일치 맥락 열에서 제거.
오더 수 900 → **330**(36개월 그대로, 시간순 테스트 100건). 캘리브레이션 게이트 20/20 유지.

8 seed 재측정 — 전부 `review_needed` 단일 코드 기준:

| | 900건 (동시 오더) | **330건 (1공장 1오더)** |
| --- | --- | --- |
| 검토 필요 건수 (테스트) | 38.5 / 270 | 16.5 / 100 |
| 실제 상위 20% 위험 정밀도 (기저) | 46.2% (20.0%) | **37.0% (20.2%)** · 1.8배 |
| 보고 오더의 편향 공장 정밀도 (기저 유형 a) | 39.0% (39.9%) | 40.8% (42.0%) |
| 12곳 연동 시 같은 값 | 49.8% (49.3%) | 48.9% (49.9%) |
| 전체 순위상관 a=3 / a=12 | 0.664 / 0.868 | **0.519 / 0.807** |
| 2단 유형 a / b / 전체 | 0.852 / — / 0.664 | **0.848 / 0.405 / 0.526** |
| MES 증분 (2단, 유형 a) | +0.374 (8/8) | **+0.594 (8/8)** |

- 라벨이 3분의 1로 줄어 κ 추정과 전체 순위상관이 내려갔다(0.664 → 0.519). 유형 a 안에서는 0.85 로 유지 — 연동 공장의 성능은 오더 수보다 보고 유무가 결정한다.
- 편향 공장 정밀도는 여전히 기저 수준이다(코드 통합 §9 의 희석 그대로).
- ⚠ `layers_by_type.py` 에서 **2단 전체 적용(0.526)이 혼합(a:2단 · b:1단 L0, 0.433)보다 높다**(−0.092, 1/8). 채점 파이프라인은 혼합 C 를 쓰고 있다 — 1공장 1오더 데이터에서 혼합 C 와 2단 전체 적용을 다시 비교해야 한다(`mixed_calibration.py` 미재실행).
- 미재실행: `sensitivity.py`(7축 28조건) · `mixed_calibration.py`. 문서 §4~§9 의 900건 기준 숫자는 기록으로 남긴다.

---

## 11. 09-08 저녁 — 네 소스 전부 · 목표 재정의(제조 품질 · 검수 품질) · 모델 벤치

**전제**(사용자): FactoryOS 에 MES·CellOS·ERP·포지 기록이 12곳 전부에서 온다. 유형 구분 폐기. 공장 차이는 보고 품질 4축.
**목표**(사용자): ① 공장 안에서 품질 좋게 제조되고 있는가(내부 불량률 p) ② 자체 검수가 얼마나 걸러 올리는가(검출률 d). 데이터·모델 설계는 바꿔도 된다. 딥러닝 허용(24시간 배치).

### 11-1. 분해 — κ 를 둘로 가른다 (`models/factory_params.py`)

κ = (1−d)/b 는 둘을 합친 비다. ERP 가 오면 가를 수 있다:
MES 보고 잡은 불량 R = p·d·b·N · ERP−Cell 물리 폐기 S = p·d·s·N · 우리 입고검사 K ~ Binom(n, p(1−d)).
s(잡은 불량 중 폐기 비중)는 **가정값 0.115**(생성기 0.03~0.20 평균) — 이 하나가 세 값의 눈금을 정한다.
b = R/(S/s̄) · e = 유출률 · p = S/(s̄N) + e · d = (S/s̄)/((S/s̄) + eN). 오더별 p = (R/b + 사전 · 150)/(N + 150) + e.

8 seed 검증(진실과의 Spearman): **오더 p +0.90** · 공장 p_f +0.87 · **검출률 d +0.49**(12곳) · 정직도 b +0.56 · 분해 escape p·(1−d) +0.71.
검출률 순위가 낮은 이유는 라벨 희소성이다 — 진짜 잡은 불량 수를 알려 줘도 d 순위는 0.42(5 seed 오라클). 12곳 검출률이 0.957~0.99 로 좁다.
폐기 보고 기반 정직도(R_scrap/S, 가정 불필요)는 폐기 보고가 포화(shrink+0.35 ≥ 1)해 +0.15 — 쓰지 않는다.

### 11-2. 유출 예측기 벤치 (`scripts/model_bench.py`, 8 seed, 테스트 100건)

| 후보 | 순위상관 | ±sd | 1위 |
| --- | --- | --- | --- |
| **2단 (보고 × κ)** | **+0.787** | 0.061 | 7/8 |
| HistGradientBoosting (poisson, exposure 가중) | +0.451 | 0.209 | 0/8 |
| MLP (torch, binomial NLL, 2×32, early stop) | +0.425 | 0.340 | 0/8 |
| 앙상블 (log 평균) | +0.682 | 0.122 | 1/8 |

네 소스가 되면서 2단이 0.526(직전) → **0.787**. 딥러닝·트리는 라벨 330건(불합격 대부분 0)에서 2단에 못 미치고 분산이 크다.
채점 파이프라인은 2단 전체 적용(`run._predict`). 혼합 C 는 폐기.

### 11-3. 검토 필요 임계 재조정 (`scripts/review_threshold_sweep.py`, 8 seed → `docs/_검토임계_sweep.md`)

12곳 전부 보고하면서 오더 단독 불일치 축(학습 분위 0.75)이 전 오더에 걸려 검토 필요가 100건 중 **34.9건**, 실제 위험 정밀도 27.3%(기저 20.2%), 불일치 축의 편향 공장 정밀도 51.9%(기저 ≈ 50%)였다.

| 설정 | 검토 필요 /100 | 실제 위험 정밀도 | 재현율 | 불일치축 편향공장 정밀도 | 헛걸음 아닌 비율 |
| --- | --- | --- | --- | --- | --- |
| 이전 (call 0.75 · visit 0.80 · trust 0.75) | 34.9 | 27.3% | 47.6% | 51.9% | 63.7% |
| **채택 (call 끔 · visit 0.90 · trust 0.75)** | **14.5** | **56.5%** | 40.8% | **57.4%** | **79.4%** |
| 더 조임 (call 끔 · visit 0.95 · trust 0.85) | 12.0 | 66.8% | 39.6% | 47.5% | 83.2% |

오더 단독 불일치 축은 건수만 늘리고 정밀도를 더하지 않았다 — §2 의 실측("한 오더의 불일치는 노이즈, 공장 누적이 신호")과 같은 결론. 끈다.
더 조이면 불일치 축 정밀도가 기저 아래로 내려가므로 0.90/0.75 에서 멈춘다. `discrepancy_flag` 도 같은 임계(0.90)를 쓴다.

### 11-4. 출력 계약 추가 필드

`predicted_internal_defect_rate` · `factory_internal_defect_rate` · `factory_detection_rate` · `factory_report_honesty` (`supplier-risk-score.v1`). `reason_primary` 에 `wip_mismatch`.
